{{#each posts as |post|}}
    <article>
        <h2>{{post.title}}</h2>
        <p>{{post.content}}</p>
        <p>Posted by {{post.user.username}} on {{format_date post.createdAt}}</p>
    </article>
{{else}}
    <p>No posts yet!</p>
{{/each}}
